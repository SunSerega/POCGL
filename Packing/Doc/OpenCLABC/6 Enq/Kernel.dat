


# Kernel.Exec1(CommandQueue<integer>, array of CommandQueue<KernelArg>)
# Kernel.Exec2(CommandQueue<integer>, CommandQueue<integer>, array of CommandQueue<KernelArg>)
# Kernel.Exec3(CommandQueue<integer>, CommandQueue<integer>, CommandQueue<integer>, array of CommandQueue<KernelArg>)
# Kernel.Exec(CommandQueue<array of UIntPtr>, CommandQueue<array of UIntPtr>, CommandQueue<array of UIntPtr>, array of CommandQueue<KernelArg>)



# KernelCommandQueue.AddExec1(CommandQueue<integer>, array of CommandQueue<KernelArg>)
# KernelCommandQueue.AddExec2(CommandQueue<integer>, CommandQueue<integer>, array of CommandQueue<KernelArg>)
# KernelCommandQueue.AddExec3(CommandQueue<integer>, CommandQueue<integer>, CommandQueue<integer>, array of CommandQueue<KernelArg>)
# KernelCommandQueue.AddExec(CommandQueue<array of UIntPtr>, CommandQueue<array of UIntPtr>, CommandQueue<array of UIntPtr>, array of CommandQueue<KernelArg>)


